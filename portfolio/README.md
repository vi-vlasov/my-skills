# Portfolio — Detroit: Become Human style

Одностраничное портфолио, воспроизводящее главное меню Detroit: Become Human:
крупный портрет андроида с живой лицевой анимацией, LED-индикатор на виске,
горизонтальное меню внизу с синей плашкой выбранного пункта и уголками-скобками,
рассыпанная HUD-телеметрия, засвеченная белая студия с вертикальными
световыми панелями.

## Технологии рендеринга

- **three.js r170** (лежит локально в `vendor/`, страница полностью автономна)
- Физически корректные материалы (PBR: skin с картой пор и clearcoat,
  глаза с clearcoat-роговицей, волосы на альфа-картах)
- Поддержка Draco, KTX2/Basis и meshopt-сжатия
- HDR image-based lighting (PMREM + RoomEnvironment)
- ACES filmic tone mapping, sRGB output
- Постобработка: UnrealBloomPass (ореол света вокруг силуэта) + OutputPass
- LED-индикатор — emissive-геометрия с HDR-цветом, посаженная на кожу
  виска raycast'ом и пульсирующая в bloom
- Схема света: тёплый key + холодный fill + два rim сзади, «плавящие»
  контур в засвеченный фон

## Управление

- Наведение мыши / ←→ — выбор пункта меню, клик / Enter — открыть, Esc — назад
- Лёгкий параллакс камеры и покачивание; полноценная реакция
  на пользователя — следующий этап

## Запуск

Из-за ES-модулей страницу нужно открывать через HTTP, не через `file://`:

```bash
cd portfolio
python3 -m http.server 8000
# открыть http://localhost:8000
```

## Как сделать своей

- Тексты секций (обо мне / проекты / навыки / опыт / контакты) — в `index.html`
- Имя, серийный номер, телеметрия — там же
- Цвета и типографика — CSS-переменные в начале `style.css`
- Камера, свет, bloom — константы в `main.js`

## Модель персонажа

Страница пробует загрузить `assets/chloe/chloe.glb` (высококачественная
модель Хлои). Эта папка **не хранится в репозитории** (`.gitignore`):
модель из игры защищена авторским правом Quantic Dream и распространять
её нельзя. Если папки нет, автоматически загружается свободная
запасная модель (`assets/facecap.glb`).

Чтобы собрать `assets/chloe/` локально из архива с FBX и TGA-текстурами:

1. FBX → GLB: `npx fbx2gltf --binary --output chloe.glb Chloe_Hostess.fbx`
2. Текстуры → веб-форматы (Pillow): albedo/normal → JPEG ≤2048,
   волосы/ресницы/брови → PNG с альфой (альфа волос — из `Chloe_Hair_A.tga`),
   roughness кожи — из зелёного канала `Chloe_Face_M.tga`,
   roughness тела — инвертированный `*_Spec.tga`.
   Имена файлов — см. `chloeMaterials()` в `main.js`.

Технические заметки: у рипа чёрные vertex colors (удаляются при загрузке)
и UDIM-смещённые UV (текстурам включён `RepeatWrapping`).

## Ассеты в репозитории

- Запасная голова: `facecap.glb` из примеров three.js (демо Face Cap,
  ARKit-блендшейпы)
- three.js — MIT License
